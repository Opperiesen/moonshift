# Controlled supervised-loop repository

This repository is a local, dependency-free Git fixture. Tests may copy it to a temporary directory
and perform only the allowlisted fixture operations; it must never be connected to a remote.
